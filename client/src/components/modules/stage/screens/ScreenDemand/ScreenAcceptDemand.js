import React, { Fragment } from 'react';
import FormAcceptDemand from '../../components/DemandForms/FormAcceptDemand';

const ScreenAcceptDemand = (props) => {
    return (
        <Fragment>
            <FormAcceptDemand {...props} />
        </Fragment>
    );
};
export default ScreenAcceptDemand;
